import type { BetterAuthOptions } from 'better-auth';

export const userConfig = {} satisfies BetterAuthOptions['user'];

export const sessionConfig = {} satisfies BetterAuthOptions['session'];

export const verificationConfig =
  {} satisfies BetterAuthOptions['verification'];

export const accountConfig = {} satisfies BetterAuthOptions['account'];
